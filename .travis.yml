language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t alextrep/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true alextrep/docker-react npm run test
